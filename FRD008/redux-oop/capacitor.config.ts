import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'step-by-step-demo',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
